# Core FastAPI service (latest compatible versions)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Document processing
docling

# Basic utilities (let pip resolve versions)
aiofiles
httpx